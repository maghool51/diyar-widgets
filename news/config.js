/**
 * Diyar Widgets — News Ticker v3.0
 * config.js
 *
 * تنها فایلی که معمولاً لازم است تغییر دهید.
 * همه‌ی تنظیمات ویجت اینجا قرار دارد و نیازی به دست‌زدن به ticker.js نیست.
 */

window.TICKER_CONFIG = {
  // -------------------------------------------------
  // منابع خبر
  // -------------------------------------------------
  // آدرس فایل JSON یا API که ساختار news.json را برمی‌گرداند
  primarySource: "news.json",

  // در صورت خطا در primarySource از این آدرس استفاده می‌شود (اختیاری)
  fallbackSource: "",

  // اگر هر دو منبع در دسترس نبودند و کش هم خالی بود،
  // این آرایه به‌عنوان «اخبار پیش‌فرض» نمایش داده می‌شود
  defaultNews: [
    {
      id: "default-1",
      title: "به ویجت اخبار دیار خوش آمدید",
      link: "#",
      category: "عمومی",
      time: "",
      source: "Diyar Widgets",
      breaking: false,
      image: ""
    }
  ],

  // -------------------------------------------------
  // ظاهر و رفتار
  // -------------------------------------------------
  selector: "#diyar-ticker",   // سلکتور المنت‌هایی که ویجت درون آن‌ها ساخته می‌شود
  speed: 40,                   // پیکسل بر ثانیه — سرعت حرکت خبرها
  direction: "auto",           // "auto" | "rtl" | "ltr"
  rtl: true,                   // پشتیبانی از راست‌به‌چپ (فارسی/عربی/اردو)
  theme: "auto",               // "auto" | "light" | "dark"
  height: 44,                  // ارتفاع نوار به پیکسل
  gap: 48,                     // فاصله بین خبرها به پیکسل
  showIcon: true,              // نمایش آیکون دسته‌بندی
  showLabel: true,             // نمایش برچسب «آخرین اخبار»
  label: "آخرین اخبار",
  pauseOnHover: true,           // توقف حرکت هنگام Hover در دسکتاپ
  pauseOnTouch: true,           // توقف حرکت هنگام لمس در موبایل

  // -------------------------------------------------
  // بروزرسانی و کش
  // -------------------------------------------------
  updateInterval: 60_000,      // فاصله بروزرسانی خودکار (میلی‌ثانیه) — گزینه‌ها: 30000, 60000, 300000, 600000
  cacheKey: "diyar_news_cache_v3",
  cacheTTL: 5 * 60_000,         // مدت اعتبار کش (میلی‌ثانیه)
  requestTimeout: 8_000,        // حداکثر زمان انتظار برای هر درخواست شبکه
  retryCount: 2,                // تعداد تلاش مجدد در صورت خطا
  retryDelay: 2_000,            // فاصله بین تلاش‌های مجدد

  // -------------------------------------------------
  // خبر فوری (Breaking News)
  // -------------------------------------------------
  breakingBlink: true,          // افکت چشمک برای اخبار فوری
  breakingLabel: "فوری",

  // -------------------------------------------------
  // دیباگ
  // -------------------------------------------------
  debug: false
};
