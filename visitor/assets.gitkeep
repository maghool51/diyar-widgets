# This file exists solely so Git tracks the otherwise-empty `assets/`
# directory. It is a placeholder for future static assets (custom
# fonts, exported design files, additional imagery, etc.) and can be
# safely deleted once real assets are added to this folder.
